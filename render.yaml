services:
  - type: web
    name: aurality-voice-assistant
    runtime: python
    buildCommand: pip install -r requirements.txt --upgrade
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false